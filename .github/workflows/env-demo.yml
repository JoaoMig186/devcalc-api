name: Env Demo

on:
  push:

jobs:
  show-env:
    runs-on: ubuntu-latest
    env:
      DEPLOY_ENV: staging

    steps:
      - name: Exibir variável de ambiente
        run: echo "Ambiente de deploy: $DEPLOY_ENV"
